logging:
  level:
    org.springdoc: INFO

spring:
  application:
    name: tfi
  profiles:
    active: dev

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5433/tfi
    username: tfi
    password: tfi
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

  mail:
    host: localhost         # MailHog
    port: 1025              # MailHog SMTP
    username: ""            # sin auth
    password: ""            # sin auth
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false
    default-encoding: UTF-8
    test-connection: false

springdoc:
  override-with-generic-response: false

server:
  port: 8080

# —— Mercado Pago ——————————————————————————————————————
mp:
  public-key: ${MP_PUBLIC_KEY}       # export MP_PUBLIC_KEY="APP_USR-..."
  access-token: ${MP_ACCESS_TOKEN}   # export MP_ACCESS_TOKEN="APP_USR-..."
  sena-percent: 0.30                 # 30% de seña